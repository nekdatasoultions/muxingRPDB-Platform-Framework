# Current-State Docs

These documents were imported from the currently deployed platform workflow so
the RPDB repo can carry the same operational references while we migrate.

Use them for:

- fresh platform bootstrap reference
- current AWS environment checks
- comparison against the newer RPDB-native customer model

Treat them as current-state references, not final RPDB-native documentation.
