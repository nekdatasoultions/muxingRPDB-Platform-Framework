# Deployment Scripts

This directory is reserved for customer-scoped deployment helpers.

Planned helpers:

- deployment preflight checks
- customer-scoped apply orchestration
- customer-scoped validation wrappers
- rollback helper entrypoints
